#include <bits/stdc++.h>
using namespace std;

int n, a, b;
char k;

int main()
{
  ios::sync_with_stdio(0);
  cin.tie(0);

  cin >> n;

  while (n--)
  {
    cin >> a >> k >> b;
  
    cout << a+b << "\n";
  }
}